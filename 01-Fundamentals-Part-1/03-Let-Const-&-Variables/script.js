let age = 30;
age = 31; //Declarevariable with let where a value of variable needs to be mutated at some pointof time in the code.

const birthYear = 1991;
//birthYear = 1990; //Const is used where value does't needs to be changed,hence this line will give error because its not legal

var job = 'programmer';
job = 'teacher'; //It looks similar to let but its actually different, which we will learn later.