// Recreate the 'description'
// variable from the last assignment, this time using the template literal syntax

const firstName = "Akshat";
const job = "programmer";
const birthYear = 1994;
const year = 2021;

//Using literal Syntax
const Akshat = "I'm " + firstName + ", a " + job + " " + (year - birthYear) + ", years old ";
console.log(Akshat);